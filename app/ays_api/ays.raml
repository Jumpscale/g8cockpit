#%RAML 1.0
title: ays rest
baseUri: http://pla.pla/{version}
version: v1

/project:
    description: a project
    get:
        description: list all projects
    post:
        description: create a new project
        body:
            application/json:
                properties:
                    path:
                        type: string
    delete:
        description: delete a project
        queryParameters:
            path:
                description: the path of the ays project
                type: string
                required: true

/blueprint:
    description: a blueprint inside a project
    get:
        description: list all blueprint
        queryParameters:
            path:
                description: the path of the ays project
                type: string
                required: true
    post:
        description: create a new blueprint
        queryParameters:
            path:
                description: the path of the ays project
                type: string
                required: true
        body:
            application/json:
                properties:
                    name:
                        type: string
    /{name}:
        get:
            description: get a blueprint
            queryParameters:
                path:
                    description: the path of the ays project
                    type: string
                    required: true
        delete:
            description: delete blueprint
            queryParameters:
                path:
                    description: the path of the ays project
                    type: string
                    required: true

/service:
    description: a service
    get:
        description: list all services
        queryParameters:
            path:
                description: the path of the ays project
                type: string
                required: true
    /{name}:
        get:
            description: get a services
            queryParameters:
                path:
                    description: the path of the ays project
                    type: string
                    required: true
        /actionlist:
            get:
                description: get the action list of a services
                queryParameters:
                    path:
                        description: the path of the ays project
                        type: string
                        required: true
        /{action}:
            post:
                description: perform an action on a services
                queryParameters:
                    path:
                        description: the path of the ays project
                        type: string
                        required: true

/template:
    description: a template of a service
    get:
        description: list all templates
        queryParameters:
            path:
                description: the path of the ays project
                type: string
                required: true
    post:
        description: create new template
        queryParameters:
            path:
                description: the path of the ays project
                type: string
                required: true
        body:
            application/json:
                properties:
                    name:
                        type: string
    /{name}:
        get:
            description: get a template
            queryParameters:
                path:
                    description: the path of the ays project
                    type: string
                    required: true
